//! # Subnet Service
//!
//! Business logic for subnet operations.

/// Service for subnet operations
pub struct SubnetService;

impl SubnetService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SubnetService {
    fn default() -> Self {
        Self::new()
    }
}
